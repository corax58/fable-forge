import React from "react";

interface Props {
  params: {
    storyId: string;
  };
}

const NodeEditorPage = ({ params }: Props) => {
  return <div>NodeEditorPage</div>;
};

export default NodeEditorPage;
